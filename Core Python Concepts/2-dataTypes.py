name = "John"
age = 30

# TypeError: can only concatenate str (not "int") to str
print(name + ' is ' + str(age) + ' years old.')

# Other data types: integers, floats, strings, booleans, lists, tuples, dictionaries, sets, None